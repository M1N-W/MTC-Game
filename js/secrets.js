'use strict';

// Public fallback for local/static hosts. Deployment may overwrite this file
// with FIREBASE_CONFIG from repository secrets before publishing.
window.CONFIG_SECRETS = window.CONFIG_SECRETS || {};
