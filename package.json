{
  "name": "mtc-game",
  "version": "3.44.9",
  "private": true,
  "scripts": {
    "build:firebase": "esbuild js/firebase-init.js --bundle --outfile=js/firebase-bundle.js --format=iife --platform=browser",
    "gen:sw": "node scripts/gen-sw-manifest.js",
    "test:smoke": "python tests/ui_smoke.py"
  },
  "dependencies": {
    "firebase": "^11.6.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
